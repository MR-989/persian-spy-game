{
  "name": "persian-spy-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multiplayer Persian (Farsi, RTL) spy game — single-service build.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client && node scripts/copy-dist.js && npm install --prefix server",
    "start": "node server/index.js",
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "npx --yes concurrently -k -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\""
  },
  "engines": {
    "node": ">=18"
  }
}
